package wy.lcsc.beans;
public class Transfer {
/*
	CREATE TABLE `rocket_transfer` (
	  `transfer_id` int(20) NOT NULL AUTO_INCREMENT,
	  `transfer_code` varchar(255) NOT NULL COMMENT '快递单号',
	  `transfer_start_time` datetime NOT NULL COMMENT '业务发起投递时间',
	  `transfer_body` longtext NOT NULL,
	  `transfer_src_sys` varchar(255) NOT NULL COMMENT '源系统',
	  `transfer_des_sys` varchar(255) NOT NULL COMMENT '目标系统',
	  `transfer_des_url` varchar(255) NOT NULL COMMENT '目的地Url',
	  `topic_id` int(255) DEFAULT NULL COMMENT '排序主题ID',
	  `message_id` int(10) DEFAULT NULL COMMENT '本地消息表ID',
	  `transfer_status` varchar(255) NOT NULL DEFAULT 'waiting' COMMENT '投递状态,waiting,done,fail',
	  `handle_by` varchar(255) DEFAULT NULL COMMENT '处理节点',
	  `start_time` datetime DEFAULT CURRENT_TIMESTAMP,
	  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
	  PRIMARY KEY (`id`)
	) ENGINE=InnoDB DEFAULT CHARSET=utf8;
 */
}
